# 砂王記 -SAND KING SAGA- 仕様書

- **フォルダ**: `235-sand-king-saga`（旧 `235-day059`。2026-09-12 命名）
- **公開URL**: https://titan11111.github.io/235-sand-king-saga/
- **1ファイル構成**: `index.html`（旧 `sandking.html`。公開要件のため改名）

## これは何か

メガドライブ様式（320×224・9bit量子化＋Bayerディザ）で描く、砂漠一本道の3DアクションRPG。
プレイヤー「タイタン」がオアシスの老人の依頼を受け、水晶を集め、遺跡を経て砂の王を討つ。

## 技術仕様

| 項目 | 内容 |
|---|---|
| 描画 | Three.js r128（CDN）／仮想解像度 320×224 ＋ **内部解像度スケール SS（最大3＝960×672）** のレンダーターゲット → ポストプロセスで9bit量子化＋4×4 Bayerディザ |
| 内部解像度 | `SS = clamp(ceil(表示倍率 × DPR), 1, 3)`。ゲームロジック・UIレイアウトの単位は 320×224 のまま固定し、**描画バッファだけ**を端末の実ピクセルへ寄せる。ディザは `vUv*(320,224)` で刻むため、SSを上げても市松の粒は 320×224 相当のまま＝メガドライブ様式は崩さない。実測でフレーム時間が90フレーム平均24ms超なら自動で1段下げる |
| 音 | WebAudio によるYM2612風FM合成。**音声ファイルは使用しない** |
| 地形 | `baseHeight()` の合成正弦波 ＋ `FLATS`（オアシス・遺跡・ボス穴の平坦化／窪み） |
| 当たり判定 | 円柱コライダー配列（`addCollider`）で押し出し解決 |
| 保存 | なし（1セッション完結） |

## ワールド定義

| 地点 | 座標 | 備考 |
|---|---|---|
| 始まりのオアシス | (0, 0) r22 | 水面半径 `r×0.34 = 7.48`、草リング外縁 `9.08` |
| 月影のオアシス | (66, -72) r17 | |
| 骨のオアシス | (-78, 58) r17 | |
| 遺跡 RUINS | (95, 78) r24 | 石柱10本＋基壇 |
| 砂の穴 PIT | (-98, -96) r30 | ボス出現地点。地形を 3.5 掘り下げ |

### 配置の鉄則（2026-09-12 制定）

**老人・プレイヤー初期位置は必ず「草リングの外（>9.08）・ヤシの内側（<12.1）」の帯に置く。**

| 定数 | 値 | 中心からの距離 |
|---|---|---|
| `POND_K` | 0.34 | — |
| `POND_R0` | 7.48 | 水面 |
| `SHORE_R0` | 9.08 | 草リング外縁 |
| `ELDER` | (4, -11) | **11.70** |
| `SPAWN_POS` | (4.5, 10.5) | **11.42** |

座標は1箇所で定義し、NPC生成・コライダー・クエストのコンパス目標・リスポーンが同じ定数を参照する
（旧実装は `3,-4` が4箇所にベタ書きで、湖の中に立っていた）。

### ピラミッド型天幕

`PYR_R = 6.4` / `PYR_H = 7.4`（旧 2.6 / 2.8 ＝ 約2.4倍）。腰回りの段・入口・金の冠石を持つ。
コライダー半径 `PYR_R×0.72 = 4.61` のため、**老人・初期位置から 5.4 以上離して置く**こと。
現配置: `tent(14,-9)` `tent(-15,-7)`。

## 操作

| | キーボード | タッチ |
|---|---|---|
| 移動 | WASD / 矢印 | 左スティック |
| 視点 | Q / E / ドラッグ | ◀ ▶ / 画面ドラッグ |
| 斬る | SPACE / J | 斬 |
| 話す・調べる | F | 話 |
| 音切替 | M | 右上 ♪（状態は `tg.235.muted` に保存） |
| ポーズ | Esc / P | 右上 Ⅱ ／ PAUSEの「つづける」 |

視点・移動の左右は画面の左右と一致する（スティック左＝画面左へ歩く。◀ / Q = 左を見る、▶ / E = 右を見る。2026-09-13 カメラ右軸で再固定）。

## 音声

| 種別 | 実体 | 備考 |
|---|---|---|
| フィールドBGM | `audio/dark-action-theme.m4a` | AAC-LC 96kbps。開始タップで unlock してループ |
| ボスBGM | `audio/neon-rush.m4a` | 砂の王出現で切替。撃破後はフィールドへ戻す |
| SE | WebAudio の YM2612 風 FM | ファイル失敗時の BGM フォールバックにも使う |
| ミュート | 右上 ♪ と M | `localStorage` キー `tg.235.muted`（`'1'`=消音） |

元ファイルは Opus-in-m4a で iOS Safari が再生できないため、AAC-LC に変換して `audio/` へ置いた。ルートの未参照 m4a は削除済み。

## コントロールパネル（iOS要件・2026-09-12 新設／2026-09-13 追記）

- ゲーム画面は **上75%**、コントロールパネルは **画面下25%** を固定占有
  （`height: calc(25vh + env(safe-area-inset-bottom))`、下限 132px）。**PCでも常時表示**
- パネルは 320×224 のスケール対象**外**。CSSピクセル実寸で親指に合わせる
- 幅は3グループを % で固定配分（30 / 28 / 38 ＋ padding 4 = 100%）。**どの端末幅でも横に溢れない**
- 高さは `height:min(100%, Nvw, Npx)` で行内に収める。横持ちでパネルが低くなっても潰れない
- 全ボタン 44px 以上（Apple HIG）。320px幅端末はメディアクエリで配分を組み替えて維持
- 縦持ちスマホでは 0.5刻みスナップを解除し、実数スケールで**横幅を使い切る**（PCはスナップ維持）
- 右上にミュート（♪）とポーズ（Ⅱ）を **スケール対象外の固定ボタン** として置く（44px）。PAUSE 中も操作できる
  （タッチ時はパネル直上、PCは画面右下。コンパス／HP枠と重ねない）
- 仮想ボタンは Pointer Events + `setPointerCapture`。押下は `pointerdown` で即反応し、短い振動を返す
- 明示ポーズあり。`visibilitychange` では BGM を止め、復帰時に再開する

## iOS対応

- `viewport-fit=cover` / `maximum-scale=1.0` / `apple-mobile-web-app-capable`
- ダブルタップ拡大の封じ込め（350ms以内の2回目 `touchend` を preventDefault）
- ピンチズーム封じ（`gesturestart` / `gesturechange` / `gestureend`）
- 長押しメニュー封じ（`contextmenu` / `-webkit-touch-callout:none`）
- バウンススクロール封じ（`position:fixed` ＋ `overscroll-behavior:none`）
- WebAudio unlock: 初回のユーザー操作と同一イベント内で `resume()` ＋ 無音バッファ再生
- BGM は `<audio>` を **pointerdown** で `.play()` する（click だと iOS がジェスチャと認めない）
- `orientationchange` / `visualViewport.resize` で再レイアウト

## クエスト進行

0. オアシスの老人に話しかける
1. サバクサソリを倒して水晶を3つ集める
2. 遺跡へ向かう
3. 砂の穴で砂の王を討つ
4. 老人へ報告

## 既知の制約

- **Three.js を cdnjs から読む**。オフライン・CDN遮断時は起動しない（未解決・タイタン判断待ち）
- `MeshLambertMaterial` に `flatShading` を渡しているが r128 では無効。コンソール警告が約50件出る
  （エラーではない。意図した低ポリのフラットシェードにするなら `MeshPhongMaterial` へ要変更）
- 320×224 固定のため、縦長スマホでは上下に黒帯が残る（様式上の仕様）
