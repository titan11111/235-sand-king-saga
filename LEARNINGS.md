# LEARNINGS — 235-sand-king-saga（2026-09-12）

タイタン指摘4点の改修記録。証跡は Playwright 実測のみ、自己申告は採らない。

## 1. 「湖に老人が入っている」の根因は座標のベタ書き4箇所

老人の座標 `3, -4` が **NPC生成 / コライダー / クエストのコンパス目標 / default分岐** の4箇所に
別々に書かれていた。中心からの距離は 5.0、水面半径は 7.48。つまり**最初から水の中**。

- 学び: 「同じ意味の数値が3箇所以上にある」時点で、どれか1つはいずれ現実と食い違う。
  `ELDER` / `SPAWN_POS` / `POND_K` / `SHORE_R0` を定数化し、**水面半径から岸を計算**する形にした。
  以後は水面の大きさを変えても老人は自動的に岸の外に留まる。
- プレイヤー初期位置 `(4,8)` も中心から 8.94 ＝ **草リング(9.08)の内側**で、水際に立っていた。
  指摘されたのは老人だけだったが、同じ原因なので同時に直した。

## 2. 大きくしたら、大きくした分だけ通れなくなる

ピラミッド型天幕を 2.6 → 6.4（約2.4倍）にしたところ、コライダー半径が 2.1 → 4.61 に育ち、
**元の位置 `(6,-7)` のままだと老人 `(4,-11)` がコライダーの内側に入り、話しかけられなくなる**。

- 学び: 見た目のスケール変更は、当たり判定を通じて**動線の変更**になる。
  拡大後に `tent(14,-9)` `tent(-15,-7)` へ退避させ、
  「老人・初期位置から 5.4 以上離す」を SPEC.md の鉄則として明文化した。

## 3. コントロールパネルは「スケールする層」に置いてはいけない

旧実装は仮想パッドを 320×224 のUIレイヤ内に置いていた。
このレイヤはレターボックスで縮小されるため、iPhone では**ボタンが実測数十pxまで潰れ、
画面中央付近に浮いて親指が届かない**。

- 対処: パネルをスケール対象外の `position:fixed` 層へ出し、画面下25%を固定占有させた。
  ゲーム画面は残り75%に収める。
- 学び: **「ゲームの解像度」と「指の解像度」は別物**。前者はスケールしてよいが、後者は実寸で固定する。

### レイアウトで踏んだ罠を3つとも実測で潰した

| 罠 | 症状 | 対処 |
|---|---|---|
| flex + `gap` + 実寸幅 | 合計が端末幅を超え、`斬` の右端が 429px（画面390px）へ**画面外へ脱出** | 3グループを % 固定配分（30/28/38＋padding4＝100%）に変更。溢れが数学的に起きない |
| `height:min(100%,…)` | 親がauto高さのため％が解決せず、**スティックが4pxに潰れた** | `#pad` を `align-items:stretch`、各グループに `height:100%` を付与 |
| % は content-box 基準 | グループ内合計がわずかに超え、`話`と`▶`が**3px重なった** | vw係数とgapを詰めて再実測 |

- 学び: CSSレイアウトは「たぶん収まる」で終わらせると必ず端末幅のどこかで壊れる。
  **5サイズ（320/375/390/430/横持ち）で はみ出し・重なり・44px未満 を機械判定**する検証を書いた。
  320px幅だけ ◀▶ が 38px になったのもこれで検出し、メディアクエリで配分を組み替えた。

## 4. 自分の修正で自分がリグレッションを出した

`SOUND: ON` 表示をタッチ時に右上へ寄せたら、**コンパス枠と重なった**。
スクリーンショットを見るまで気づかなかった。

- 学び: 数値検証（はみ出し・重なり判定）を書いていた**当の自分が、検証対象に入れていない要素で事故った**。
  以後は重なり判定に HUD・コンパス・クエスト枠も含める。今回は事後に追加して PASS を確認済み。

## 5. harness のタップ失敗は「当たり判定を塞ぐUI」のサイン

`FAIL タップ: locator.tap Timeout` の原因は、タイトル画面 `#title` が `pointer-events:auto` で
**Canvas中央のヒットテストを塞いでいた**こと。

- タイトル送りは `window` の click ハンドラが全画面で処理しているため、`#title` に
  `pointer-events` は**そもそも不要だった**。`pe` クラスを外して解決。
- 学び: harness の失敗は「テストの都合」ではなく実害だった。
  レターボックス部分のタップ経路も、この修正で素直になった。

## 検証証跡（2026-09-12）

```
_tools/game-harness.mjs 235-sand-king-saga  → RESULT PASS
  index.html存在 / 幅390 / Canvas320x224 / 61 RAF/秒 /
  タップ成功 / コンソールエラー0 / リクエスト失敗0 / 0.06MB
```

機能テスト（iPhone 13 エミュレート・10項目）全PASS:
起動時title / タップで開始 / 黒帯タップで送り / メッセージ送り完了 / スティックで移動 /
斬ボタン / 打感クラス / 老人は湖の外 / 初期位置も湖の外 / ピラミッド拡大

レイアウトテスト（5サイズ）: はみ出し0・重なり0・44px未満0

## 6. 未参照BGMは「ある」だけでは鳴らない。しかも Opus は iPhone で死ぬ

ルートに `Dark Action Theme.m4a` と `Neon Rush.m4a` が置いてあったが、コードは WebAudio 合成しか呼んでいなかった。
さらにコンテナは m4a でも中身は **Opus**。iOS Safari はこれを再生しない。

- 対処: AAC-LC 96kbps へ変換して `audio/` に置き、開始タップで `play()` する。
  フィールド=`dark-action-theme.m4a`、ボス=`neon-rush.m4a`。SE は従来の FM。
  ファイル再生に失敗したときだけ合成BGMへ落ちる。
- 学び: 「ファイルがある」と「再生経路がある」は別。iPhone主戦場では **コーデックまで確認**する。
  拡張子だけで判断すると、実機で無音になる。

## 7. 左右が逆だったのは、strafe ベクトルがカメラ右と逆だった

符号を推測でひっくり返し続けたのが原因。スポーン `yaw=π` で実測すると:

- カメラ右（`matrixWorld` 列0）= `+X`、画面投影も右
- 旧コードの strafe 右 = `-X`（カメラと逆）
- スティック左の移動 · カメラ右 = `+1` → **左入力が画面右へ歩く**
- `yaw -=`（旧◀）の前方変化 · カメラ右 = `+` → **◀ が右を見る**

- 対処: strafe をカメラ右に合わせ `rx=-cos(yaw), rz=sin(yaw)`。◀/Q は `yaw +=`、▶/E は `yaw -=`。ドラッグ右は `yaw -=`。
- 学び: 3人称の左右は **カメラのワールド右軸を測ってから** 入力を載せる。符号の当てずっぽうは禁止。

## 8. ミュートとポーズは、スケールする HUD に置いてはいけない

SOUND 表示は 320×224 レイヤ内にあった。指で押すボタン（ミュート／ポーズ）は
パネルと同じく `position:fixed` の実寸へ出した。状態は `tg.235.muted` に保存。

## 9. 斬撃が蠍に当たらないのは「正面の扇」が噛みつき位置を外していた

旧判定は `dot < 0.25`（カメラ前方 約±75°）だけ。サソリの攻撃距離は 2.4 で、
横に回り込んで噛みついてくる。**殴られている距離なのに扇の外**で、斬が全部空振りになる。

加えてサソリの頭はモデルの +X に付いているのに `rotation.y = yaw`（+Z 向き）だったので、
見た目の胴体が判定中心から 90° ずれていた。

- 対処: 近距離（噛みつき圏）は全方位。外側はカメラ向きかキャラ向きのどちらかで足りる。
  リーチ 3.6→4.2。サソリは `yaw - π/2` で頭を相手へ。
- 学び: 敵の攻撃距離 ≤ プレイヤーの有効斬撃距離、を数字で見る。
  「アニメは出るが当たらない」は演出の問題ではなく **扇の角度** であることが多い。

## 検証証跡（2026-09-13）

```
_tools/game-harness.mjs 235-sand-king-saga  → RESULT PASS
  index.html存在 / 幅390 / Canvas320x224 / 61 RAF/秒 /
  タップ成功 / コンソールエラー0 / リクエスト失敗0 / 1.96MB
```

機能確認（Chromium・iPhone 390×844・hasTouch）:
パッド表示 / 全操作ボタン44px以上 / ミュート・ポーズがコンパス・HUD・パッドと非重なり /
タップで開始 / フィールドBGM `audio/dark-action-theme.m4a` をリクエスト /
ポーズ表示→再開で閉じる / ミュートで ♪→× / ページエラー0

公開実体サイズ（.git除外）: 4.15MB（20MB鉄則内）

左右軸の再実測（Chromium・開始ダイアログ閉じたあと `update(0.2)`）:
スティック左・A · カメラ右 = `-2.2`（画面左へ歩く）／ D = `+2.2` ／
◀ 前方変化 · カメラ右 = `-0.51`（左旋回）／ ▶・右ドラッグは正。PASS。
iPhone 実機の左右確認は未実施のまま公開。

## 未解決（タイタン判断待ち）

1. **Three.js の CDN 依存**: `cdnjs.cloudflare.com` から読む。遮断・オフラインで起動不能。
2. **flatShading 警告 約50件**: `MeshLambertMaterial` は r128 で `flatShading` 非対応。
   エラーではないが、意図した低ポリ表現にするには `MeshPhongMaterial` への変更が要る。
3. **index.html が 1000行超**: 今回は挙動修正を優先し、style/script 分割は未実施。
